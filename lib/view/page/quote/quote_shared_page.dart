import 'package:flutter/material.dart';

class QuoteSharePage extends StatelessWidget {
  const QuoteSharePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
