import 'package:flutter/material.dart';

class RegisterViewContainer extends StatelessWidget {
  const RegisterViewContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
